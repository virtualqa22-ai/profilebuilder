apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: profilebuilder-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: profilebuilder.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: profilebuilder-service
            port:
              number: 80